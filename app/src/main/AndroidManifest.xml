<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movietracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieTracker">
        <activity android:name=".MovieImage"></activity>
        <activity android:name=".RatingResults" />
        <activity android:name=".APIResult" />
        <activity android:name=".Ratings" />
        <activity android:name=".Search" />
        <activity android:name=".EditMovieDetails" />
        <activity android:name=".Favourites" />
        <activity android:name=".EditMovie" />
        <activity android:name=".DisplayMovies" />
        <activity android:name=".RegisterMovie" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>